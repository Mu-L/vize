# P2-9 — Core transforms as S2 passes (series record)

**An explicitly marked small series** (the plan README's permitted
exception): one reviewable PR per transform, all under this ID. This
record grows one installment section per landed PR; the task's box in
`phase-2.md` stays unticked until the series completes.

## Installment table

| #   | landed     | scope                                                                                             |
| --- | ---------- | ------------------------------------------------------------------------------------------------- |
| 1   | 2026-08-21 | the series substrate (`vize_ricalco::pass`, lane flag, differential comparator) + the `v-if` port |
| 2   | 2026-08-21 | the `v-for` port: hygiene consumption as the pass body, the for projection in the comparator      |

Per-installment records live in [p2-9/](./p2-9/), split under the 350-line
source budget - one file per installment, appended as the series lands:

- [Installment 1 - substrate + `v-if`](./p2-9/installment-1.md)
- [Installment 2 - `v-for`](./p2-9/installment-2.md)
