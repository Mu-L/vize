# P5-13 — Manifest-keyed JS plugin result caching

Contract: [phase-5-tasks-later.md](../phase-5-tasks-later.md#p5-13--js-plugin-caching).
Implemented by [#6417](https://github.com/ubugeeei-prod/vize/pull/6417) on
2026-09-23; acceptance reconciled on 2026-09-26 against the released
`28cfd679406f9522df7cda47c07e5f96292d53a6` source.

## Result

`lintWithPlugins` shares a manifest-checked `plugin.result` artifact key between
its opt-in process cache and optional `cacheDir` store. The S0 content key
covers the complete SFC because diagnostic positions are file-absolute. The
P5-1b row declares the host build, compiled features, filename and plugin
identity, version, code, visited node kinds, demanded facts, and plugin-owned
configuration or ambient values. The key implementation and its documentation
are checked together by `key_manifests`.

Cache opt-in requires `cacheInputs`, including an explicit empty list for a
pure rule. Duplicate or empty names are refused. Changing a captured value
invalidates independently of the function source. The host build identity
combines the Git revision and a hash of the plugin source closure and lockfile,
so a same-version host rebuild also invalidates. Unknown node kinds or demands
are refused before cache lookup.

A disk hit returns the exact mapped diagnostics. Writes use a temporary file
and atomic rename; absent, torn, or stale entries miss and are replaced. The
same result survives a new cache instance and a separate Node process.

## Acceptance evidence

[Full Check 36217796037](https://github.com/ubugeeei-prod/vize/actions/runs/36217796037)
succeeded on the exact released source on 2026-09-26. Both `clippy-and-test`
and `test-scripts` succeeded, covering the following witnesses:

- `cargo test -p vize_davinci --test key_manifests`: all declared ambient inputs
  affect the key; insertion order does not; missing or extra declarations and
  an incorrect content stage are refused; the documented rows match the code.
- `cargo test -p vize_vitrine --lib plugin_sdk_host`: exact batch and diagnostic
  oracles, key input coverage, build identity and captured-value invalidation,
  disk reuse, torn/stale entry refusal, and demand refusal.
- `tests/tooling/davinci-plugin-sdk.test.ts`: deterministic diagnostics,
  content-key hits, version/code misses, cross-process disk persistence,
  captured configuration invalidation across processes, and manifest refusal
  before a hit. The cache preserves the independent `EXPECTED` diagnostic set.

P5-13 acceptance is met. SDK packaging and the other hook families belong to
P6-7; resident RSS and summary-firewall work remain their own gates.
